# Core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0

# ML (torch installed separately as CPU-only in Dockerfile)
transformers>=4.40.0
safetensors>=0.4.0

# Google Cloud Storage
google-cloud-storage>=2.10.0

# Optional: ONNX acceleration (uncomment for ~2× CPU speedup)
# optimum[onnxruntime]>=1.19.0
